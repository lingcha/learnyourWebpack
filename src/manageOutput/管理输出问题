问题：使用分离的入口，就要在html文件中添加更多script标签，并且由于使用hash来控制文件名，文件名会变得冗长。加上你可能需要手动删除掉dist下的资源文件。这都会让管理输出变得很不容易。
例子：修改的html文件在示例重现的过程应该放在dist目录下，并且这个文件夹的js文件也是webpack配置的入口文件

解决办法：使用htmlwebpackplugin管理输出，这个插件会帮助我们重新生成资源文件，包括js文件，html文件。并对已有的同名文件进行替换。
